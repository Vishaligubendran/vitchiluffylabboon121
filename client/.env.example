# Vercel / production: point to your deployed API
# Local dev: /api uses Vite proxy to http://localhost:3000
VITE_API_URL=/api

# Same as GOOGLE_CLIENT_ID on the backend
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
